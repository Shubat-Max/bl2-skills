
/**
 * Maximum hero level that cannot be exceeded
 * Includes all the DLC updates by 08/2019
 */
export const maxHeroLvl = 80;

/**
 * Hero level required for main skill become active and skill tree access enabled
 */
export const treeAccessLvl = 5;

/**
 * Points required to be spent on a current tier to enable next tier
 */
export const tierProgressPoints = 5;
